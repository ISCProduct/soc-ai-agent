# AGENTS.md

SOC AI Agent（採用支援SaaS: Go + Next.js + Python RAG）。
アーキテクチャ・コマンド・コード規約は `CLAUDE.md`、詳細設計は `docs/wiki/` を参照する。

## Code Review Rules

指摘は**日本語**で書き、根拠となるファイル・行と、壊れる具体的なケースを示す。

### DB / マイグレーション
- スキーマ変更は `Backend/migrations/` に **up と down の両方**があるか。GORM AutoMigrate は禁止。
- `docs/wiki/migrations.md` の手順（連番・ロールバック可否）から外れていないか。

### 認証・権限・秘密情報
- `/admin` 配下に認証ミドルウェアが掛かっているか。ルートを追加して認証から漏れていないか（過去の再発ポイント）。
- APIキー・Webhook URL・トークンをコード / ログ / 例外メッセージ / テストに残していないか。
- GitHub Actions は `permissions` を最小で明示し、secret は必要なステップにだけ渡しているか。コンテナは非rootで動かしているか。

### API の一貫性
- 一覧APIの `limit` 上限やページング・エラー形式が、既存の同種APIと揃っているか。
- 入力バリデーションが信頼境界（HTTPハンドラ）にあるか。

### フロントエンド
- `any` を使っていないか。失敗を握りつぶして成功扱いにしていないか（エラーはユーザーに見せる）。
- 非同期処理で**後着の応答が新しい結果を上書きする競合**がないか。検索・絞り込み・タブ切替は特に確認する。

### スコア / マッチング
- スコアは `user_weight_scores`（10カテゴリ×4フェーズ）。マッチングは `UserWeightScore × CompanyWeightProfile`。この前提を崩す変更は影響範囲を明示させる。

### RAG / Python
- 依存追加は `constraints.txt` にピン留めしているか。プロンプト変更は版管理されているか。LLM出力のJSONを検証しているか。

### テスト
- 振る舞いを変えた箇所に、壊れたら落ちるテストが1つあるか。
- テストが本番と同じ経路を通っているか（モックで本番コードを迂回して「通っているだけ」になっていないか）。

### 指摘しないこと
- 好みレベルの命名・整形、差分と無関係なリファクタ提案、既存コードの全面書き換え。
- CodeRabbit が同じPRで既に挙げている内容の繰り返し。
