-- #929 のロールバック。
--
-- 表記揺れは複数の旧名が同じ正典名へ寄るため（例: 問題解決力・分析思考 → 技術志向）、
-- 元の名前は復元できない。データを壊さないよう、down では何もしない。
-- 正典化そのものはアプリ側のバリデーション(ParseWeightCategory)とも整合しており、
-- 旧名へ戻すと保存経路で弾かれるため、戻す意味も無い。
SELECT 1;
